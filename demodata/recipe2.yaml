fixtures:
  environment:
    PLANET: Earth

  git_url: https://github.com/RedHatQE/newa.git
  git_ref: ks_compose_event
  tmt_path: demodata
  plan: /plan2

dimensions:
  cities:
    - environment:
        CITY: Brno
      when: CONTEXT.color is match("blue")
    - environment:
        CITY: Boston
  colors:
    - context:
        color: red
      when: ENVIRONMENT.CITY is not match("Brno")
    - context:
        color: blue
